// Placeholder stub — the original file was empty (0 bytes) and untracked, so
// there was no committed version to restore. This renders nothing and exists
// only to satisfy the `ChatFollowUpSuggestions` import in chat-composer.tsx so
// the app can load. Replace with the real follow-up-suggestions implementation.
export function ChatFollowUpSuggestions(_props: { visible: boolean }) {
	return null;
}
